$TTL			38400
@			IN	SOA	gollum.segulja.com.	admin.segulja.com. (
			7						; serial number
			10800				; refresh
			3600				; retry
			38400				; expire
			604800 )		; negative cache TTL

; name servers - NS records
				IN		NS		gollum.

; PTR records
1       IN    PTR   bree.segulja.com.
